const demoData = {
  es: {
    manufactura: {
      gerencial: {
        chat: [
          'ISO Coach: Que objetivo financiero quieres proteger este trimestre?',
          'Lider: Reducir merma y retrabajos sin frenar despachos.',
          'ISO Coach: Perfecto. Voy a traducir esto a objetivo de calidad y control de proceso.',
        ],
        twin: [
          ['Motor IA', 'Perfil Organizacional activo'],
          ['Oportunidad de ahorro', 'Merma por lote -12%'],
          ['Objetivo prioritario', 'Reducir retrabajo en linea critica'],
          ['Mapa de proceso', 'Produccion -> Inspeccion -> Liberacion'],
        ],
        progress: 24,
      },
      tecnico: {
        chat: [
          'ISO Coach: Cual es tu proceso con mayor variacion de no conformidades?',
          'Usuario: Ensamble final por cambios de especificacion.',
          'ISO Coach: Generando matriz causa-riesgo y control documental asociado.',
        ],
        twin: [
          ['Motor IA', 'Impacto y Ahorro activo'],
          ['Riesgo principal', 'Desviacion de especificacion en ensamble'],
          ['Control propuesto', 'Validacion de instruccion versionada'],
          ['Salida ISO', 'Registro de trazabilidad por lote'],
        ],
        progress: 27,
      },
    },
    servicios: {
      gerencial: {
        chat: [
          'ISO Coach: Donde pierdes clientes por variabilidad del servicio?',
          'Lider: En tiempos de respuesta y seguimiento de incidentes.',
          'ISO Coach: Vamos a convertirlo en objetivo SLA + experiencia de cliente.',
        ],
        twin: [
          ['Motor IA', 'Proposito y Alineacion activo'],
          ['Indicador clave', 'Reclamos por mil atenciones'],
          ['Objetivo prioritario', 'Reducir tiempo medio de respuesta'],
          ['Accion inmediata', 'Flujo estandar de atencion critica'],
        ],
        progress: 22,
      },
      tecnico: {
        chat: [
          'ISO Coach: Que evidencia falta para cerrar auditoria interna?',
          'Usuario: Registros de seguimiento de casos y analisis de causa.',
          'ISO Coach: Preparando estructura de evidencia y plan CAPA.',
        ],
        twin: [
          ['Motor IA', 'Impacto y Ahorro activo'],
          ['Gap de evidencia', 'Seguimiento de casos incompleto'],
          ['Plan ISO', 'Formato CAPA + cierre por responsable'],
          ['Hito', 'Auditoria interna lista en 4 semanas'],
        ],
        progress: 25,
      },
    },
    distribucion: {
      gerencial: {
        chat: [
          'ISO Coach: Que impacto te genera el quiebre de stock en rentabilidad?',
          'Lider: Perdemos ventas y sube el costo logistico por urgencias.',
          'ISO Coach: Te propongo objetivo OTIF y tablero de alertas operativas.',
        ],
        twin: [
          ['Motor IA', 'Perfil Organizacional activo'],
          ['Indicador clave', 'OTIF por cliente prioritario'],
          ['Oportunidad de ahorro', 'Costo logistico por pedido -8%'],
          ['Flujo base', 'Compra -> Recepcion -> Despacho'],
        ],
        progress: 23,
      },
      tecnico: {
        chat: [
          'ISO Coach: Existe trazabilidad completa desde recepcion hasta devolucion?',
          'Usuario: Parcial, depende del centro de distribucion.',
          'ISO Coach: Generando mapa de puntos de control y riesgo operativo.',
        ],
        twin: [
          ['Motor IA', 'Proposito y Alineacion activo'],
          ['Riesgo principal', 'Trazabilidad incompleta por bodega'],
          ['Control sugerido', 'Checklist unico de recepcion y devolucion'],
          ['Prioridad', 'Estandarizar codificacion de lote'],
        ],
        progress: 26,
      },
    },
  },
  en: {
    manufactura: {
      gerencial: {
        chat: [
          'ISO Coach: Which financial objective do you need to protect this quarter?',
          'Leader: Reduce scrap and rework without slowing down shipments.',
          'ISO Coach: Good. I will turn that into a quality objective and process control plan.',
        ],
        twin: [
          ['AI Engine', 'Organizational Profile active'],
          ['Savings opportunity', 'Scrap per batch -12%'],
          ['Priority objective', 'Reduce rework on critical line'],
          ['Process map', 'Production -> Inspection -> Release'],
        ],
        progress: 24,
      },
      tecnico: {
        chat: [
          'ISO Coach: Which process shows the highest variation in nonconformities?',
          'User: Final assembly when specifications change.',
          'ISO Coach: Building cause-risk matrix and linked document controls.',
        ],
        twin: [
          ['AI Engine', 'Impact and Savings active'],
          ['Main risk', 'Specification drift in assembly'],
          ['Suggested control', 'Versioned work instruction validation'],
          ['ISO output', 'Batch traceability record'],
        ],
        progress: 27,
      },
    },
    servicios: {
      gerencial: {
        chat: [
          'ISO Coach: Where are you losing clients because service delivery varies?',
          'Leader: Response times and incident follow-up.',
          'ISO Coach: Let us turn that into an SLA plus customer experience objective.',
        ],
        twin: [
          ['AI Engine', 'Purpose and Alignment active'],
          ['Key indicator', 'Claims per thousand service cases'],
          ['Priority objective', 'Reduce mean response time'],
          ['Immediate action', 'Standard critical-care workflow'],
        ],
        progress: 22,
      },
      tecnico: {
        chat: [
          'ISO Coach: What evidence is still missing to close the internal audit?',
          'User: Case follow-up records and cause analysis.',
          'ISO Coach: Preparing evidence structure and CAPA plan.',
        ],
        twin: [
          ['AI Engine', 'Impact and Savings active'],
          ['Evidence gap', 'Incomplete case follow-up'],
          ['ISO plan', 'CAPA template plus accountable closure'],
          ['Milestone', 'Internal audit ready in 4 weeks'],
        ],
        progress: 25,
      },
    },
    distribucion: {
      gerencial: {
        chat: [
          'ISO Coach: How does stockout risk affect profitability?',
          'Leader: We lose sales and logistics costs rise due to urgent orders.',
          'ISO Coach: I recommend an OTIF objective and operations alert board.',
        ],
        twin: [
          ['AI Engine', 'Organizational Profile active'],
          ['Key indicator', 'OTIF by priority customer'],
          ['Savings opportunity', 'Logistics cost per order -8%'],
          ['Base flow', 'Purchase -> Receipt -> Dispatch'],
        ],
        progress: 23,
      },
      tecnico: {
        chat: [
          'ISO Coach: Do you have full traceability from receipt to return?',
          'User: Only partially, depending on the distribution center.',
          'ISO Coach: Generating map of control points and operational risk.',
        ],
        twin: [
          ['AI Engine', 'Purpose and Alignment active'],
          ['Main risk', 'Incomplete traceability by warehouse'],
          ['Suggested control', 'Single receipt and return checklist'],
          ['Priority', 'Standardize lot coding'],
        ],
        progress: 26,
      },
    },
  },
  pt: {
    manufactura: {
      gerencial: {
        chat: [
          'ISO Coach: Qual objetivo financeiro voce precisa proteger neste trimestre?',
          'Lider: Reduzir perdas e retrabalho sem atrasar expedicoes.',
          'ISO Coach: Perfeito. Vou transformar isso em objetivo de qualidade e controle de processo.',
        ],
        twin: [
          ['Motor IA', 'Perfil Organizacional ativo'],
          ['Oportunidade de economia', 'Perda por lote -12%'],
          ['Objetivo prioritario', 'Reduzir retrabalho na linha critica'],
          ['Mapa de processo', 'Producao -> Inspecao -> Liberacao'],
        ],
        progress: 24,
      },
      tecnico: {
        chat: [
          'ISO Coach: Qual processo tem maior variacao de nao conformidades?',
          'Usuario: Montagem final quando mudam as especificacoes.',
          'ISO Coach: Gerando matriz causa-risco e controles documentais associados.',
        ],
        twin: [
          ['Motor IA', 'Impacto e Economia ativo'],
          ['Risco principal', 'Desvio de especificacao na montagem'],
          ['Controle sugerido', 'Validacao de instrucao versionada'],
          ['Saida ISO', 'Registro de rastreabilidade por lote'],
        ],
        progress: 27,
      },
    },
    servicios: {
      gerencial: {
        chat: [
          'ISO Coach: Onde voce perde clientes pela variabilidade do servico?',
          'Lider: No tempo de resposta e no acompanhamento de incidentes.',
          'ISO Coach: Vamos transformar isso em objetivo de SLA e experiencia do cliente.',
        ],
        twin: [
          ['Motor IA', 'Proposito e Alinhamento ativo'],
          ['Indicador chave', 'Reclamacoes por mil atendimentos'],
          ['Objetivo prioritario', 'Reduzir tempo medio de resposta'],
          ['Acao imediata', 'Fluxo padrao para atendimento critico'],
        ],
        progress: 22,
      },
      tecnico: {
        chat: [
          'ISO Coach: Que evidencia falta para fechar a auditoria interna?',
          'Usuario: Registros de acompanhamento de casos e analise de causa.',
          'ISO Coach: Preparando estrutura de evidencias e plano CAPA.',
        ],
        twin: [
          ['Motor IA', 'Impacto e Economia ativo'],
          ['Lacuna de evidencia', 'Acompanhamento de casos incompleto'],
          ['Plano ISO', 'Modelo CAPA com fechamento por responsavel'],
          ['Marco', 'Auditoria interna pronta em 4 semanas'],
        ],
        progress: 25,
      },
    },
    distribucion: {
      gerencial: {
        chat: [
          'ISO Coach: Como a ruptura de estoque afeta a rentabilidade?',
          'Lider: Perdemos vendas e o custo logistico sobe por urgencias.',
          'ISO Coach: Proponho objetivo OTIF e painel de alertas operacionais.',
        ],
        twin: [
          ['Motor IA', 'Perfil Organizacional ativo'],
          ['Indicador chave', 'OTIF por cliente prioritario'],
          ['Oportunidade de economia', 'Custo logistico por pedido -8%'],
          ['Fluxo base', 'Compra -> Recebimento -> Despacho'],
        ],
        progress: 23,
      },
      tecnico: {
        chat: [
          'ISO Coach: Existe rastreabilidade completa do recebimento ate a devolucao?',
          'Usuario: Parcial, depende do centro de distribuicao.',
          'ISO Coach: Gerando mapa de pontos de controle e risco operacional.',
        ],
        twin: [
          ['Motor IA', 'Proposito e Alinhamento ativo'],
          ['Risco principal', 'Rastreabilidade incompleta por armazem'],
          ['Controle sugerido', 'Checklist unico de recebimento e devolucao'],
          ['Prioridade', 'Padronizar codificacao de lote'],
        ],
        progress: 26,
      },
    },
  },
};
  const industryCards = {
    es: {
      manufactura: {
        title: 'Manufactura: menos merma, mas capacidad util',
        description: 'IsoSmart conecta calidad de proceso con costo por lote para que la mejora continua tenga impacto financiero claro.',
        bullets: [
          'KPI central: costo de no calidad por linea',
          'Objetivo sugerido: retrabajo por orden de produccion',
          'Resultado buscado: estabilidad operativa y margen protegido',
        ],
      },
      servicios: {
        title: 'Servicios: estandarizar experiencia sin perder velocidad',
        description: 'El sistema ayuda a convertir variabilidad de atencion en procesos repetibles con trazabilidad y responsabilidad clara.',
        bullets: [
          'KPI central: reclamos por mil atenciones',
          'Objetivo sugerido: tiempo de respuesta a incidencias',
          'Resultado buscado: retencion de clientes y reputacion',
        ],
      },
      distribucion: {
        title: 'Importacion y distribucion: control de promesa y costo logistico',
        description: 'La IA identifica cuellos operativos y prioriza acciones para proteger OTIF y evitar reprocesos de bodega.',
        bullets: [
          'KPI central: OTIF por segmento',
          'Objetivo sugerido: costo logistico por pedido',
          'Resultado buscado: cumplimiento de entrega con menor variacion',
        ],
      },
    },
    en: {
      manufactura: {
        title: 'Manufacturing: less scrap, more usable capacity',
        description: 'IsoSmart links process quality with batch cost so continuous improvement produces clear financial impact.',
        bullets: [
          'Core KPI: cost of poor quality per line',
          'Suggested objective: rework per production order',
          'Desired outcome: operational stability and protected margin',
        ],
      },
      servicios: {
        title: 'Services: standardize experience without losing speed',
        description: 'The system turns service variability into repeatable processes with traceability and clear accountability.',
        bullets: [
          'Core KPI: claims per thousand service cases',
          'Suggested objective: response time to incidents',
          'Desired outcome: client retention and reputation',
        ],
      },
      distribucion: {
        title: 'Import and distribution: protect delivery promise and logistics cost',
        description: 'AI identifies operational bottlenecks and prioritizes actions to protect OTIF and reduce warehouse rework.',
        bullets: [
          'Core KPI: OTIF by segment',
          'Suggested objective: logistics cost per order',
          'Desired outcome: delivery compliance with lower variability',
        ],
      },
    },
    pt: {
      manufactura: {
        title: 'Manufatura: menos perdas, mais capacidade util',
        description: 'A IsoSmart conecta qualidade do processo com custo por lote para gerar impacto financeiro claro.',
        bullets: [
          'KPI central: custo da nao qualidade por linha',
          'Objetivo sugerido: retrabalho por ordem de producao',
          'Resultado esperado: estabilidade operacional e margem protegida',
        ],
      },
      servicios: {
        title: 'Servicos: padronizar experiencia sem perder velocidade',
        description: 'O sistema converte variabilidade de atendimento em processos repetiveis com rastreabilidade e responsabilidade clara.',
        bullets: [
          'KPI central: reclamacoes por mil atendimentos',
          'Objetivo sugerido: tempo de resposta a incidentes',
          'Resultado esperado: retencao de clientes e reputacao',
        ],
      },
      distribucion: {
        title: 'Importacao e distribuicao: controlar promessa e custo logistico',
        description: 'A IA identifica gargalos operacionais e prioriza acoes para proteger OTIF e evitar retrabalho no deposito.',
        bullets: [
          'KPI central: OTIF por segmento',
          'Objetivo sugerido: custo logistico por pedido',
          'Resultado esperado: cumprimento de entrega com menor variacao',
        ],
      },
    },
  };

  const experimentText = {
    a: {
      es: { label: 'A Financiero', eyebrow: 'Smart3AI | crecimiento rentable con IA aplicada' },
      en: { label: 'A Financial', eyebrow: 'Smart3AI | profitable growth with applied AI' },
      pt: { label: 'A Financeiro', eyebrow: 'Smart3AI | crescimento rentavel com IA aplicada' },
    },
    b: {
      es: { label: 'B Técnico', eyebrow: 'Smart3AI | precisión técnica ISO + ejecución operativa' },
      en: { label: 'B Technical', eyebrow: 'Smart3AI | ISO technical precision plus operational execution' },
      pt: { label: 'B Tecnico', eyebrow: 'Smart3AI | precisao tecnica ISO + execucao operacional' },
    },
  };

  const landingContent = {
    es: {
      personaTabCeo: 'Dueño / Gerencia',
      personaTabQuality: 'Lider de Calidad',
      personaTabOps: 'Operaciones',
      metricOnboarding: 'de onboarding consultivo',
      metricAi: 'priorizados por IA',
      metricEcosystem: 'ecosistema B2B escalable',
      demoTitle: 'Demo ISO Coach',
      demoSub: 'Selecciona el lenguaje y sector para simular el onboarding',
      demoLivePill: 'En vivo',
      toneLabel: 'Lenguaje',
      toneOptionManagerial: 'Gerencial',
      toneOptionTechnical: 'Tecnico',
      industryLabel: 'Industria',
      industryOptionManufacturing: 'Manufactura',
      industryOptionServices: 'Servicios',
      industryOptionDistribution: 'Importacion / Distribucion',
      coachPanelTitle: 'ISO Coach',
      digitalTwinTitle: 'Gemelo Digital ISO',
      progressLabel: 'Sistema base construido',
      tabManufacturing: 'Manufactura',
      tabServices: 'Servicios',
      tabDistribution: 'Importacion / Distribucion',
      isoTwinScope: 'Alcance del SGC v1',
      isoTwinMap: 'Mapa de procesos inicial',
      isoTwinRisks: 'Top 5 riesgos/oportunidades',
      isoTwinPolicy: 'Politica de calidad borrador',
      isoTwinRoadmap: 'Roadmap 3-6-12 meses',
      isoTwinScopeStatus: 'Generado',
      isoTwinMapStatus: 'Generado',
      isoTwinRisksStatus: 'Generado',
      isoTwinPolicyStatus: 'Generado',
      isoTwinRoadmapStatus: 'Generado',
      isoIntentExploreTitle: 'Revisar encaje por industria',
      isoIntentExploreCta: 'Comparar casos',
      isoIntentEvaluateTitle: 'Demo con dirección y calidad',
      isoIntentEvaluateCta: 'Agendar sesión conjunta',
      isoIntentActivateTitle: 'Inicio inmediato del onboarding',
      isoIntentActivateCta: 'Solicitar activación',
    },
    en: {
      personaTabCeo: 'Owner / Leadership',
      personaTabQuality: 'Quality Lead',
      personaTabOps: 'Operations',
      metricOnboarding: 'of consultative onboarding',
      metricAi: 'prioritized by AI',
      metricEcosystem: 'scalable B2B ecosystem',
      demoTitle: 'ISO Coach Demo',
      demoSub: 'Select tone and sector to simulate onboarding',
      demoLivePill: 'Live',
      toneLabel: 'Tone',
      toneOptionManagerial: 'Managerial',
      toneOptionTechnical: 'Technical',
      industryLabel: 'Industry',
      industryOptionManufacturing: 'Manufacturing',
      industryOptionServices: 'Services',
      industryOptionDistribution: 'Import / Distribution',
      coachPanelTitle: 'ISO Coach',
      digitalTwinTitle: 'ISO Digital Twin',
      progressLabel: 'Base system built',
      tabManufacturing: 'Manufacturing',
      tabServices: 'Services',
      tabDistribution: 'Import / Distribution',
      isoTwinScope: 'QMS scope v1',
      isoTwinMap: 'Initial process map',
      isoTwinRisks: 'Top 5 risks/opportunities',
      isoTwinPolicy: 'Draft quality policy',
      isoTwinRoadmap: '3-6-12 month roadmap',
      isoTwinScopeStatus: 'Generated',
      isoTwinMapStatus: 'Generated',
      isoTwinRisksStatus: 'Generated',
      isoTwinPolicyStatus: 'Generated',
      isoTwinRoadmapStatus: 'Generated',
      isoIntentExploreTitle: 'Review industry fit',
      isoIntentExploreCta: 'Compare cases',
      isoIntentEvaluateTitle: 'Demo with leadership and quality',
      isoIntentEvaluateCta: 'Book joint session',
      isoIntentActivateTitle: 'Immediate onboarding start',
      isoIntentActivateCta: 'Request activation',
    },
    pt: {
      personaTabCeo: 'Direcao / Gerencia',
      personaTabQuality: 'Lider de Qualidade',
      personaTabOps: 'Operacoes',
      metricOnboarding: 'de onboarding consultivo',
      metricAi: 'priorizados por IA',
      metricEcosystem: 'ecossistema B2B escalavel',
      demoTitle: 'Demo ISO Coach',
      demoSub: 'Selecione tom e setor para simular o onboarding',
      demoLivePill: 'Ao vivo',
      toneLabel: 'Tom',
      toneOptionManagerial: 'Gerencial',
      toneOptionTechnical: 'Tecnico',
      industryLabel: 'Industria',
      industryOptionManufacturing: 'Manufatura',
      industryOptionServices: 'Servicos',
      industryOptionDistribution: 'Importacao / Distribuicao',
      coachPanelTitle: 'ISO Coach',
      digitalTwinTitle: 'Gemeo Digital ISO',
      progressLabel: 'Sistema base construido',
      tabManufacturing: 'Manufatura',
      tabServices: 'Servicos',
      tabDistribution: 'Importacao / Distribuicao',
      isoTwinScope: 'Escopo do SGQ v1',
      isoTwinMap: 'Mapa inicial de processos',
      isoTwinRisks: 'Top 5 riscos/oportunidades',
      isoTwinPolicy: 'Rascunho da politica da qualidade',
      isoTwinRoadmap: 'Roadmap 3-6-12 meses',
      isoTwinScopeStatus: 'Gerado',
      isoTwinMapStatus: 'Gerado',
      isoTwinRisksStatus: 'Gerado',
      isoTwinPolicyStatus: 'Gerado',
      isoTwinRoadmapStatus: 'Gerado',
      isoIntentExploreTitle: 'Revisar aderencia por industria',
      isoIntentExploreCta: 'Comparar casos',
      isoIntentEvaluateTitle: 'Demo com direcao e qualidade',
      isoIntentEvaluateCta: 'Agendar sessao conjunta',
      isoIntentActivateTitle: 'Inicio imediato do onboarding',
      isoIntentActivateCta: 'Solicitar ativacao',
    },
};

const toneMap = {
  gerencial: 'gerencial',
  tecnico: 'tecnico',
};

const personaHero = {
  es: {
    ceo: {
      title: 'Haz que ISO 9001 deje de ser costo y empiece a proteger tu margen.',
      lead: 'Si hoy no tienes visibilidad del costo de no calidad, estás perdiendo rentabilidad. IsoSmart alinea sistema de gestión con dinero, clientes y ejecución en 30 minutos.',
      primaryText: 'Agendar demo ejecutiva',
      primaryHref: 'mailto:felipe@smart3ai.com?subject=Demo%20ejecutiva%20Smart3AI',
      secondaryText: 'Evaluar impacto financiero',
      secondaryHref: '#resultado-financiero',
    },
    quality: {
      title: 'Pasa de checklist de auditoría a sistema ISO ejecutable con evidencia viva.',
      lead: 'IsoSmart estructura alcance, procesos, riesgos, política y roadmap desde onboarding conversacional.',
      primaryText: 'Ver demo técnico',
      primaryHref: '#demo',
      secondaryText: 'Solicitar sesión para calidad',
      secondaryHref: 'mailto:felipe@smart3ai.com?subject=Demo%20para%20Lider%20de%20Calidad',
    },
    ops: {
      title: 'Estandariza operación y reduce fricción entre áreas con IA aplicada.',
      lead: 'Smart3AI conecta estrategia y ejecución para que operaciones, calidad y dirección trabajen con objetivos medibles.',
      primaryText: 'Ver rutas por industria',
      primaryHref: '#industrias',
      secondaryText: 'Solicitar sesión operativa',
      secondaryHref: 'mailto:felipe@smart3ai.com?subject=Demo%20para%20Operaciones',
    },
  },
  en: {
    ceo: {
      title: 'Turn ISO 9001 from overhead into margin protection.',
      lead: 'If you cannot see the cost of poor quality today, you are already leaking profitability. IsoSmart aligns your quality system with revenue, customer value, and execution in 30 minutes.',
      primaryText: 'Book executive demo',
      primaryHref: 'mailto:felipe@smart3ai.com?subject=Executive%20Demo%20Smart3AI',
      secondaryText: 'Estimate financial impact',
      secondaryHref: '#resultado-financiero',
    },
    quality: {
      title: 'Move from audit checklist to an executable ISO system with live evidence.',
      lead: 'IsoSmart structures scope, process map, risks, policy, and roadmap through conversational onboarding.',
      primaryText: 'View technical demo',
      primaryHref: '#demo',
      secondaryText: 'Request quality session',
      secondaryHref: 'mailto:felipe@smart3ai.com?subject=Demo%20for%20Quality%20Leads',
    },
    ops: {
      title: 'Standardize operations and reduce cross-team friction with applied AI.',
      lead: 'Smart3AI connects strategy and operations so leadership, quality, and operations act on one measurable execution model.',
      primaryText: 'See industry tracks',
      primaryHref: '#industrias',
      secondaryText: 'Request operations session',
      secondaryHref: 'mailto:felipe@smart3ai.com?subject=Demo%20for%20Operations',
    },
  },
  pt: {
    ceo: {
      title: 'Transforme a ISO 9001 de custo em protecao de margem.',
      lead: 'Se voce nao enxerga o custo da nao qualidade hoje, ja esta perdendo rentabilidade. A IsoSmart conecta qualidade, receita e execucao em 30 minutos.',
      primaryText: 'Agendar demo executiva',
      primaryHref: 'mailto:felipe@smart3ai.com?subject=Demo%20Executiva%20Smart3AI',
      secondaryText: 'Avaliar impacto financeiro',
      secondaryHref: '#resultado-financiero',
    },
    quality: {
      title: 'Saia do checklist de auditoria para um sistema ISO executavel com evidencias.',
      lead: 'A IsoSmart estrutura escopo, mapa de processos, riscos e roadmap com onboarding conversacional.',
      primaryText: 'Ver demo tecnico',
      primaryHref: '#demo',
      secondaryText: 'Solicitar sessao de qualidade',
      secondaryHref: 'mailto:felipe@smart3ai.com?subject=Demo%20para%20Qualidade',
    },
    ops: {
      title: 'Padronize operacoes e reduza friccao entre areas com IA aplicada.',
      lead: 'A Smart3AI conecta estrategia e operacao para que lideranca, qualidade e operacoes executem com metas claras.',
      primaryText: 'Ver trilhas por industria',
      primaryHref: '#industrias',
      secondaryText: 'Solicitar sessao operacional',
      secondaryHref: 'mailto:felipe@smart3ai.com?subject=Demo%20para%20Operacoes',
    },
  },
};

const experimentConfig = {
  a: {
    key: 'A',
    label: 'A Financiero',
    eyebrow: 'Smart3AI | crecimiento rentable con IA aplicada',
    defaultPersona: 'ceo',
  },
  b: {
    key: 'B',
    label: 'B Técnico',
    eyebrow: 'Smart3AI | precisión técnica ISO + ejecución operativa',
    defaultPersona: 'quality',
  },
};

const i18n = {
  es: {
    nav_ecosystem: 'Ecosistema',
    nav_onboarding: 'Onboarding IA',
    nav_industries: 'Industrias',
    nav_profiles: 'Perfiles',
    nav_finance: 'Resultado financiero',
    adminapps_access: 'Acceso AdminApps',
    view_product: 'Ver producto',
    book_demo: 'Agendar demo',
    theme_toggle: 'Modo oscuro',
    iso_nav_capabilities: 'Capacidades',
    iso_nav_flow: 'Flujo',
    iso_nav_activation: 'Activacion',
    iso_back_home: 'Volver a landing',
    iso_request_demo: 'Solicitar demo',
    iso_eyebrow: 'Producto B2B especializado',
    iso_h1: 'IsoSmart: producto de Smart3AI para ejecutar ISO con impacto financiero.',
    iso_lead: 'IsoSmart no solo organiza cumplimiento: transforma tu sistema de gestion en un motor de decisiones, ahorro y cuidado del cliente. Todo el diseño de producto prioriza claridad, trazabilidad y ejecución continua para equipos ISO.',
    iso_activate_trial: 'Activar prueba guiada',
    iso_view_onboarding: 'Ver onboarding conversacional',
    iso_eval_roi: 'Evaluar ROI con mi equipo',
    iso_value_30: 'Resumen de valor en 30 minutos',
    iso_onboarding_results: 'Resultados que recibe el equipo al cerrar onboarding',
    iso_cta_title: 'Activa IsoSmart y entra con una sesion guiada desde el dia 1.',
    iso_cta_desc: 'Implementacion preparada para mercado latinoamericano: claridad comercial, soporte operativo y resultados trazables.',
    iso_start_now: 'Iniciar ahora',
    iso_check_pricing: 'Consultar pricing',
  },
  en: {
    nav_ecosystem: 'Ecosystem',
    nav_onboarding: 'AI Onboarding',
    nav_industries: 'Industries',
    nav_profiles: 'Profiles',
    nav_finance: 'Financial impact',
    adminapps_access: 'AdminApps Access',
    view_product: 'View product',
    book_demo: 'Book demo',
    theme_toggle: 'Dark mode',
    iso_nav_capabilities: 'Capabilities',
    iso_nav_flow: 'Flow',
    iso_nav_activation: 'Activation',
    iso_back_home: 'Back to corporate',
    iso_request_demo: 'Request demo',
    iso_eyebrow: 'Specialized B2B product',
    iso_h1: 'IsoSmart: Smart3AI product to execute ISO with financial impact.',
    iso_lead: 'IsoSmart goes beyond compliance management: it turns your quality system into a decision, savings, and customer-care engine. Product experience prioritizes clarity, traceability, and continuous execution for ISO teams.',
    iso_activate_trial: 'Start guided trial',
    iso_view_onboarding: 'View conversational onboarding',
    iso_eval_roi: 'Assess ROI with my team',
    iso_value_30: '30-minute value summary',
    iso_onboarding_results: 'What teams receive after onboarding',
    iso_cta_title: 'Activate IsoSmart and start with a guided session from day one.',
    iso_cta_desc: 'Built for Latin American operations: commercial clarity, operational support, and traceable outcomes.',
    iso_start_now: 'Start now',
    iso_check_pricing: 'Check pricing',
  },
  pt: {
    nav_ecosystem: 'Ecossistema',
    nav_onboarding: 'Onboarding IA',
    nav_industries: 'Industrias',
    nav_profiles: 'Perfis',
    nav_finance: 'Impacto financeiro',
    adminapps_access: 'Acesso AdminApps',
    view_product: 'Ver produto',
    book_demo: 'Agendar demo',
    theme_toggle: 'Modo escuro',
    iso_nav_capabilities: 'Capacidades',
    iso_nav_flow: 'Fluxo',
    iso_nav_activation: 'Ativacao',
    iso_back_home: 'Voltar ao corporativo',
    iso_request_demo: 'Solicitar demo',
    iso_eyebrow: 'Produto B2B especializado',
    iso_h1: 'IsoSmart: produto da Smart3AI para executar ISO com impacto financeiro.',
    iso_lead: 'A IsoSmart vai alem da conformidade e transforma seu sistema de qualidade em um motor de decisoes e economia.',
    iso_activate_trial: 'Ativar prova guiada',
    iso_view_onboarding: 'Ver onboarding conversacional',
    iso_eval_roi: 'Avaliar ROI com meu time',
    iso_value_30: 'Resumo de valor em 30 minutos',
    iso_onboarding_results: 'Resultados recebidos ao finalizar o onboarding',
    iso_cta_title: 'Ative a IsoSmart e comece com sessao guiada desde o primeiro dia.',
    iso_cta_desc: 'Implementacao pronta para a America Latina com resultados rastreaveis.',
    iso_start_now: 'Comecar agora',
    iso_check_pricing: 'Consultar preco',
  },
};

const uiText = {
  es: {
    variant: 'Variante',
    noNewEvents: 'Sin eventos nuevos por sincronizar',
    syncOkTitle: 'Sincronizado',
    syncOkText: (created, campaign) => `${created} eventos enviados (${campaign})`,
    syncErrTitle: 'Error de sincronizacion',
    syncStatusOk: (created, campaign) => `Sincronizado: ${created} eventos (${campaign})`,
    syncStatusErr: (message) => `Error al sincronizar con AdminApps: ${message}`,
    tie: 'Empate tecnico',
    winningA: 'Ganando: Variante A',
    winningB: 'Ganando: Variante B',
    abStatus: (winner) => `Estado A/B: ${winner}`,
    events: 'Eventos',
    ctaClicks: 'CTA clicks',
    ctaRate: 'Tasa CTA',
    campaignActive: (value) => `Campana activa: ${value || 'direct'}`,
  },
  en: {
    variant: 'Variant',
    noNewEvents: 'No new events to sync',
    syncOkTitle: 'Synchronized',
    syncOkText: (created, campaign) => `${created} events sent (${campaign})`,
    syncErrTitle: 'Synchronization error',
    syncStatusOk: (created, campaign) => `Synchronized: ${created} events (${campaign})`,
    syncStatusErr: (message) => `AdminApps sync error: ${message}`,
    tie: 'Technical tie',
    winningA: 'Leading: Variant A',
    winningB: 'Leading: Variant B',
    abStatus: (winner) => `A/B status: ${winner}`,
    events: 'Events',
    ctaClicks: 'CTA clicks',
    ctaRate: 'CTA rate',
    campaignActive: (value) => `Active campaign: ${value || 'direct'}`,
  },
  pt: {
    variant: 'Variante',
    noNewEvents: 'Sem novos eventos para sincronizar',
    syncOkTitle: 'Sincronizado',
    syncOkText: (created, campaign) => `${created} eventos enviados (${campaign})`,
    syncErrTitle: 'Erro de sincronizacao',
    syncStatusOk: (created, campaign) => `Sincronizado: ${created} eventos (${campaign})`,
    syncStatusErr: (message) => `Erro ao sincronizar com AdminApps: ${message}`,
    tie: 'Empate tecnico',
    winningA: 'Vencendo: Variante A',
    winningB: 'Vencendo: Variante B',
    abStatus: (winner) => `Status A/B: ${winner}`,
    events: 'Eventos',
    ctaClicks: 'Cliques CTA',
    ctaRate: 'Taxa CTA',
    campaignActive: (value) => `Campanha ativa: ${value || 'direct'}`,
  },
};

function tUi() {
  return uiText[currentLanguage] || uiText.es;
}

function tLanding() {
  return landingContent[currentLanguage] || landingContent.es;
}

function applyLandingContent() {
  const dict = tLanding();
  Object.entries(dict).forEach(([id, value]) => {
    const node = document.getElementById(id);
    if (node) {
      node.textContent = value;
    }
  });
}

const adminAppsConfig = {
  endpoint: window.localStorage.getItem('smart3ai_adminapps_endpoint')
    || '/api/landing-analytics/events/',
};

let currentPersona = 'ceo';
let activeExperiment = 'A';
let analyticsSyncInFlight = false;
let currentLanguage = 'es';

function showSweetAlert({ title, text, icon = 'info' }) {
  if (window.Swal) {
    window.Swal.fire({
      title,
      text,
      icon,
      confirmButtonColor: '#2563eb',
      background: '#111a2d',
      color: '#e2e8f0',
    });
    return;
  }
  console.info('[landing-alert]', title, text);
}

function applyLanguage(lang) {
  const next = i18n[lang] ? lang : 'es';
  currentLanguage = next;
  window.localStorage.setItem('smart3ai_lang', next);
  document.documentElement.lang = next;

  const dict = i18n[next];
  document.querySelectorAll('[data-i18n]').forEach((node) => {
    const key = node.dataset.i18n;
    if (dict[key]) {
      node.textContent = dict[key];
    }
  });

  const select = document.getElementById('languageSelect');
  if (select) {
    select.value = next;
  }

  applyLandingContent();
  applyTheme(document.documentElement.dataset.theme || 'light');
  applyPersona(currentPersona);
  renderDemo();

  const activeTab = document.querySelector('.tab.active');
  if (activeTab) {
    renderIndustryPanel(activeTab.dataset.tab);
  }

  const chip = document.getElementById('abChip');
  if (chip) {
    const label = tUi().variant;
    const configKey = (window.localStorage.getItem('smart3ai_ab_variant') || 'a').toLowerCase();
    const config = experimentText[configKey]?.[currentLanguage] || experimentText.a.es;
    chip.textContent = `${label} ${config.label}`;
  }
}

function bindLanguageControl() {
  const select = document.getElementById('languageSelect');
  if (!select) {
    return;
  }

  select.addEventListener('change', () => {
    applyLanguage(select.value);
    showSweetAlert({
      title: currentLanguage === 'en' ? 'Language updated' : currentLanguage === 'pt' ? 'Idioma atualizado' : 'Idioma actualizado',
      text: currentLanguage === 'en' ? 'Interface language changed.' : currentLanguage === 'pt' ? 'Idioma da interface atualizado.' : 'Se actualizó el idioma de la interfaz.',
      icon: 'success',
    });
  });
  const saved = window.localStorage.getItem('smart3ai_lang') || 'es';
  applyLanguage(saved);
}

function applyTheme(theme) {
  const normalized = theme === 'dark' ? 'dark' : 'light';
  document.documentElement.dataset.theme = normalized;
  window.localStorage.setItem('smart3ai_theme', normalized);

  const btn = document.getElementById('themeToggle');
  if (btn) {
    btn.textContent = normalized === 'dark'
      ? (currentLanguage === 'en' ? 'Light mode' : currentLanguage === 'pt' ? 'Modo claro' : 'Modo claro')
      : (currentLanguage === 'en' ? 'Dark mode' : currentLanguage === 'pt' ? 'Modo escuro' : 'Modo oscuro');
  }
}

function bindThemeControl() {
  const btn = document.getElementById('themeToggle');
  if (!btn) {
    return;
  }

  btn.addEventListener('click', () => {
    const next = document.documentElement.dataset.theme === 'dark' ? 'light' : 'dark';
    applyTheme(next);
    showSweetAlert({
      title: currentLanguage === 'en' ? 'Theme updated' : currentLanguage === 'pt' ? 'Tema atualizado' : 'Tema actualizado',
      text: next === 'dark'
        ? (currentLanguage === 'en' ? 'Dark mode enabled.' : currentLanguage === 'pt' ? 'Modo escuro ativado.' : 'Modo oscuro activado.')
        : (currentLanguage === 'en' ? 'Light mode enabled.' : currentLanguage === 'pt' ? 'Modo claro ativado.' : 'Modo claro activado.'),
      icon: 'success',
    });
  });

  const saved = window.localStorage.getItem('smart3ai_theme');
  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
  applyTheme(saved || (prefersDark ? 'dark' : 'light'));
}

function getExperimentVariant() {
  const params = new URLSearchParams(window.location.search);
  const forced = params.get('ab');

  if (forced && (forced.toLowerCase() === 'a' || forced.toLowerCase() === 'b')) {
    const variant = forced.toLowerCase();
    window.localStorage.setItem('smart3ai_ab_variant', variant);
    return variant;
  }

  const persisted = window.localStorage.getItem('smart3ai_ab_variant');
  if (persisted === 'a' || persisted === 'b') {
    return persisted;
  }

  const randomVariant = Math.random() < 0.5 ? 'a' : 'b';
  window.localStorage.setItem('smart3ai_ab_variant', randomVariant);
  return randomVariant;
}

function ensureAnalyticsStore() {
  if (!window.smart3aiAnalytics) {
    window.smart3aiAnalytics = {
      events: [],
      syncedCount: 0,
    };
  }
}

function ensureSessionId() {
  const key = 'smart3ai_session_id';
  let sessionId = window.localStorage.getItem(key);
  if (!sessionId) {
    sessionId = `sess-${Date.now()}-${Math.random().toString(16).slice(2, 10)}`;
    window.localStorage.setItem(key, sessionId);
  }
  return sessionId;
}

function getCampaignValue() {
  const input = document.getElementById('campaignInput');
  const queryParams = new URLSearchParams(window.location.search);
  const fromQuery = (queryParams.get('campaign') || queryParams.get('utm_campaign') || '').trim();

  if (fromQuery) {
    window.localStorage.setItem('smart3ai_campaign', fromQuery);
    if (input) {
      input.value = fromQuery;
    }
    return fromQuery;
  }

  const fromInput = input ? input.value.trim() : '';
  if (fromInput) {
    window.localStorage.setItem('smart3ai_campaign', fromInput);
    return fromInput;
  }

  const persisted = (window.localStorage.getItem('smart3ai_campaign') || '').trim();
  if (persisted && input && !input.value.trim()) {
    input.value = persisted;
  }
  return persisted || 'direct';
}

function updateSyncStatus(message, isError = false) {
  const node = document.getElementById('syncStatus');
  if (!node) {
    return;
  }
  node.textContent = message;
  node.style.color = isError ? '#a11d2e' : '';
}

function trackEvent(eventName, payload = {}) {
  ensureAnalyticsStore();

  const event = {
    eventName,
    ts: new Date().toISOString(),
    experiment: activeExperiment,
    persona: currentPersona,
    sessionId: ensureSessionId(),
    pagePath: window.location.pathname,
    pageUrl: window.location.href,
    referrer: document.referrer || '',
    ...payload,
  };

  window.smart3aiAnalytics.events.push(event);
  console.info('[smart3ai-analytics]', event);
  renderAnalyticsSummary();
}

async function syncAnalyticsToAdminApps(trigger = 'manual', options = {}) {
  ensureAnalyticsStore();

  if (analyticsSyncInFlight) {
    return;
  }

  const events = window.smart3aiAnalytics.events;
  const syncedCount = Number(window.smart3aiAnalytics.syncedCount || 0);
  const pending = events.slice(syncedCount);

  if (!pending.length) {
    if (!options.silent) {
      updateSyncStatus(tUi().noNewEvents);
    }
    return;
  }

  analyticsSyncInFlight = true;
  const campaign = getCampaignValue();

  try {
    const response = await fetch(adminAppsConfig.endpoint, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      keepalive: Boolean(options.keepalive),
      body: JSON.stringify({
        campaign,
        trigger,
        events: pending,
      }),
    });

    if (!response.ok) {
      throw new Error(`HTTP ${response.status}`);
    }

    const payload = await response.json();
    const created = Number(payload.created || 0);
    window.smart3aiAnalytics.syncedCount = syncedCount + created;
    updateSyncStatus(tUi().syncStatusOk(created, campaign));
    if (!options.silent) {
      showSweetAlert({
        title: tUi().syncOkTitle,
        text: tUi().syncOkText(created, campaign),
        icon: 'success',
      });
    }
  } catch (error) {
    updateSyncStatus(tUi().syncStatusErr(error.message), true);
    if (!options.silent) {
      showSweetAlert({
        title: tUi().syncErrTitle,
        text: error.message,
        icon: 'error',
      });
    }
  } finally {
    analyticsSyncInFlight = false;
  }
}

function toCsv(events) {
  const columns = ['eventName', 'ts', 'experiment', 'persona', 'intent', 'location', 'href', 'variant', 'selectedPersona'];
  const rows = [columns.join(',')];

  events.forEach((event) => {
    const row = columns.map((column) => {
      const raw = event[column] ?? '';
      const text = String(raw).replace(/"/g, '""');
      return `"${text}"`;
    });
    rows.push(row.join(','));
  });

  return rows.join('\n');
}

function triggerDownload(fileName, content, mimeType) {
  const blob = new Blob([content], { type: mimeType });
  const url = URL.createObjectURL(blob);
  const link = document.createElement('a');
  link.href = url;
  link.download = fileName;
  document.body.appendChild(link);
  link.click();
  link.remove();
  URL.revokeObjectURL(url);
}

function renderAnalyticsSummary() {
  ensureAnalyticsStore();
  const events = window.smart3aiAnalytics.events;

  const exploreClicks = events.filter((event) => event.eventName === 'cta_click' && event.intent === 'explorar').length;
  const evaluateClicks = events.filter((event) => event.eventName === 'cta_click' && event.intent === 'evaluar').length;
  const activateClicks = events.filter((event) => event.eventName === 'cta_click' && event.intent === 'activar').length;

  const variantAEvents = events.filter((event) => event.experiment === 'A').length;
  const variantBEvents = events.filter((event) => event.experiment === 'B').length;
  const variantACtas = events.filter((event) => event.eventName === 'cta_click' && event.experiment === 'A').length;
  const variantBCtas = events.filter((event) => event.eventName === 'cta_click' && event.experiment === 'B').length;
  const variantARate = variantAEvents ? (variantACtas / variantAEvents) : 0;
  const variantBRate = variantBEvents ? (variantBCtas / variantBEvents) : 0;
  const winner = variantARate === variantBRate
    ? tUi().tie
    : variantARate > variantBRate ? tUi().winningA : tUi().winningB;

  const totalNode = document.getElementById('abTotalEvents');
  const exploreNode = document.getElementById('abExploreClicks');
  const evaluateNode = document.getElementById('abEvaluateClicks');
  const activateNode = document.getElementById('abActivateClicks');
  const variantANode = document.getElementById('abVariantA');
  const variantBNode = document.getElementById('abVariantB');

  if (totalNode) totalNode.textContent = String(events.length);
  if (exploreNode) exploreNode.textContent = String(exploreClicks);
  if (evaluateNode) evaluateNode.textContent = String(evaluateClicks);
  if (activateNode) activateNode.textContent = String(activateClicks);

  if (variantANode) {
    variantANode.textContent = `${tUi().events}: ${variantAEvents} | ${tUi().ctaClicks}: ${variantACtas} | ${tUi().ctaRate}: ${(variantARate * 100).toFixed(1)}%`;
  }

  if (variantBNode) {
    variantBNode.textContent = `${tUi().events}: ${variantBEvents} | ${tUi().ctaClicks}: ${variantBCtas} | ${tUi().ctaRate}: ${(variantBRate * 100).toFixed(1)}%`;
  }

  updateSyncStatus(tUi().abStatus(winner));

  if (events.length - Number(window.smart3aiAnalytics.syncedCount || 0) >= 5) {
    syncAnalyticsToAdminApps('auto-batch', { silent: true });
  }
}

function bindAnalyticsActions() {
  const exportJsonBtn = document.getElementById('exportJsonBtn');
  const exportCsvBtn = document.getElementById('exportCsvBtn');
  const clearAnalyticsBtn = document.getElementById('clearAnalyticsBtn');
  const syncAnalyticsBtn = document.getElementById('syncAnalyticsBtn');
  const campaignInput = document.getElementById('campaignInput');

  if (exportJsonBtn) {
    exportJsonBtn.addEventListener('click', () => {
      ensureAnalyticsStore();
      const payload = JSON.stringify(window.smart3aiAnalytics.events, null, 2);
      triggerDownload('smart3ai-ab-events.json', payload, 'application/json;charset=utf-8');
    });
  }

  if (exportCsvBtn) {
    exportCsvBtn.addEventListener('click', () => {
      ensureAnalyticsStore();
      const csv = toCsv(window.smart3aiAnalytics.events);
      triggerDownload('smart3ai-ab-events.csv', csv, 'text/csv;charset=utf-8');
    });
  }

  if (clearAnalyticsBtn) {
    clearAnalyticsBtn.addEventListener('click', () => {
      ensureAnalyticsStore();
      window.smart3aiAnalytics.events = [];
      window.smart3aiAnalytics.syncedCount = 0;
      renderAnalyticsSummary();
      console.info('[smart3ai-analytics]', { eventName: 'analytics_reset', ts: new Date().toISOString() });
    });
  }

  if (syncAnalyticsBtn) {
    syncAnalyticsBtn.addEventListener('click', async () => {
      await syncAnalyticsToAdminApps('manual');
    });
  }

  if (campaignInput) {
    campaignInput.addEventListener('change', () => {
      const value = campaignInput.value.trim();
      window.localStorage.setItem('smart3ai_campaign', value || 'direct');
      updateSyncStatus(tUi().campaignActive(value));
    });
  }

  getCampaignValue();
  renderAnalyticsSummary();
}

function renderDemo() {
  const tone = document.getElementById('toneSelect')?.value || 'gerencial';
  const industry = document.getElementById('industrySelect')?.value || 'manufactura';
  const toneKey = toneMap[tone] || 'gerencial';

  const payload = demoData[currentLanguage]?.[industry]?.[toneKey] || demoData.es[industry]?.[toneKey];
  if (!payload) {
    return;
  }

  const chatLines = document.getElementById('chatLines');
  const twinList = document.getElementById('digitalTwin');
  const progressValue = document.getElementById('progressValue');
  const progressBar = document.getElementById('progressBar');

  if (chatLines) {
    chatLines.innerHTML = '';
    payload.chat.forEach((line, index) => {
      const row = document.createElement('p');
      row.className = 'chat-line';
      row.style.animationDelay = `${index * 80}ms`;
      row.textContent = line;
      chatLines.appendChild(row);
    });
  }

  if (twinList) {
    twinList.innerHTML = '';
    payload.twin.forEach((item) => {
      const li = document.createElement('li');
      const left = document.createElement('span');
      left.textContent = item[0];
      const right = document.createElement('strong');
      right.textContent = item[1];
      li.appendChild(left);
      li.appendChild(right);
      twinList.appendChild(li);
    });
  }

  if (progressValue) {
    progressValue.textContent = `${payload.progress}%`;
  }

  if (progressBar) {
    progressBar.style.width = `${payload.progress}%`;
  }
}

function renderIndustryPanel(industry) {
  const panel = document.getElementById('industryPanel');
  const item = industryCards[currentLanguage]?.[industry] || industryCards.es[industry];

  if (!panel || !item) {
    return;
  }

  panel.innerHTML = '';

  const title = document.createElement('h3');
  title.textContent = item.title;

  const description = document.createElement('p');
  description.textContent = item.description;

  const list = document.createElement('ul');
  item.bullets.forEach((point) => {
    const li = document.createElement('li');
    li.textContent = point;
    list.appendChild(li);
  });

  panel.appendChild(title);
  panel.appendChild(description);
  panel.appendChild(list);
}

function bindIndustryTabs() {
  const tabs = Array.from(document.querySelectorAll('.tab'));
  if (!tabs.length) {
    return;
  }

  tabs.forEach((tab) => {
    tab.addEventListener('click', () => {
      tabs.forEach((item) => {
        item.classList.remove('active');
        item.setAttribute('aria-selected', 'false');
      });
      tab.classList.add('active');
      tab.setAttribute('aria-selected', 'true');

      const nextIndustry = tab.dataset.tab;
      renderIndustryPanel(nextIndustry);

      const industrySelect = document.getElementById('industrySelect');
      if (industrySelect) {
        industrySelect.value = nextIndustry;
        renderDemo();
      }
    });
  });

  const active = tabs.find((item) => item.classList.contains('active')) || tabs[0];
  renderIndustryPanel(active.dataset.tab);
}

function bindDemoControls() {
  const toneSelect = document.getElementById('toneSelect');
  const industrySelect = document.getElementById('industrySelect');

  if (toneSelect) {
    toneSelect.addEventListener('change', renderDemo);
  }

  if (industrySelect) {
    industrySelect.addEventListener('change', () => {
      const tab = document.querySelector(`.tab[data-tab="${industrySelect.value}"]`);
      if (tab) {
        tab.click();
      } else {
        renderDemo();
      }
    });
  }

  renderDemo();
}

function activateReveals() {
  const nodes = document.querySelectorAll('.reveal');
  if (!nodes.length) {
    return;
  }

  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
      }
    });
  }, {
    threshold: 0.12,
  });

  nodes.forEach((node) => observer.observe(node));
}

function applyPersona(personaKey) {
  const data = personaHero[currentLanguage]?.[personaKey] || personaHero.es[personaKey];
  if (!data) {
    return;
  }

  const heroTitle = document.getElementById('heroTitle');
  const heroLead = document.getElementById('heroLead');
  const heroPrimaryCta = document.getElementById('heroPrimaryCta');
  const heroSecondaryCta = document.getElementById('heroSecondaryCta');

  if (heroTitle) heroTitle.textContent = data.title;
  if (heroLead) heroLead.textContent = data.lead;

  if (heroPrimaryCta) {
    heroPrimaryCta.textContent = data.primaryText;
    heroPrimaryCta.setAttribute('href', data.primaryHref);
  }

  if (heroSecondaryCta) {
    heroSecondaryCta.textContent = data.secondaryText;
    heroSecondaryCta.setAttribute('href', data.secondaryHref);
  }

  currentPersona = personaKey;
}

function applyExperimentVariant() {
  const variant = getExperimentVariant();
  const config = experimentConfig[variant] || experimentConfig.a;
  const variantText = experimentText[variant]?.[currentLanguage] || experimentText.a.es;
  activeExperiment = config.key;

  const eyebrow = document.getElementById('heroEyebrow');
  const chip = document.getElementById('abChip');
  const tabs = Array.from(document.querySelectorAll('.persona-tab'));

  if (!eyebrow && !chip && !tabs.length) {
    return;
  }

  if (eyebrow) {
    eyebrow.textContent = variantText.eyebrow;
  }

  if (chip) {
    const label = tUi().variant;
    chip.textContent = `${label} ${variantText.label}`;
  }

  tabs.forEach((tab) => {
    const isActive = tab.dataset.persona === config.defaultPersona;
    tab.classList.toggle('active', isActive);
    tab.setAttribute('aria-selected', isActive ? 'true' : 'false');
  });

  applyPersona(config.defaultPersona);
  trackEvent('experiment_assigned', {
    variant: config.key,
  });
}

function bindPersonaSwitch() {
  const tabs = Array.from(document.querySelectorAll('.persona-tab'));
  if (!tabs.length) {
    return;
  }

  tabs.forEach((tab) => {
    tab.addEventListener('click', () => {
      tabs.forEach((item) => {
        item.classList.remove('active');
        item.setAttribute('aria-selected', 'false');
      });

      tab.classList.add('active');
      tab.setAttribute('aria-selected', 'true');
      applyPersona(tab.dataset.persona);
      trackEvent('persona_switch', {
        selectedPersona: tab.dataset.persona,
      });
    });
  });
}

function bindCtaTracking() {
  const ctas = Array.from(document.querySelectorAll('[data-cta-intent]'));
  ctas.forEach((cta) => {
    cta.addEventListener('click', () => {
      trackEvent('cta_click', {
        intent: cta.dataset.ctaIntent,
        location: cta.dataset.ctaLocation || 'unknown',
        href: cta.getAttribute('href') || '',
      });
      syncAnalyticsToAdminApps('cta_click', { silent: true });
    });
  });
}

function formatUsd(value) {
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: 'USD',
    maximumFractionDigits: 0,
  }).format(value);
}

function recalcRoi() {
  const billingInput = document.getElementById('billingInput');
  const cnqInput = document.getElementById('cnqInput');
  const improvementInput = document.getElementById('improvementInput');
  const roiValue = document.getElementById('roiValue');

  if (!billingInput || !cnqInput || !improvementInput || !roiValue) {
    return;
  }

  const billing = Number.parseFloat(billingInput.value) || 0;
  const cnq = Number.parseFloat(cnqInput.value) || 0;
  const improvement = Number.parseFloat(improvementInput.value) || 0;

  const monthlyLoss = billing * (cnq / 100);
  const potentialSaving = monthlyLoss * (improvement / 100);

  roiValue.textContent = formatUsd(potentialSaving);
}

function bindRoiCalculator() {
  const inputs = ['billingInput', 'cnqInput', 'improvementInput']
    .map((id) => document.getElementById(id))
    .filter(Boolean);

  if (!inputs.length) {
    return;
  }

  inputs.forEach((input) => {
    input.addEventListener('input', recalcRoi);
    input.addEventListener('change', recalcRoi);
  });

  recalcRoi();
}

window.addEventListener('DOMContentLoaded', () => {
  bindLanguageControl();
  bindThemeControl();
  applyExperimentVariant();
  bindPersonaSwitch();
  bindIndustryTabs();
  bindDemoControls();
  bindRoiCalculator();
  bindCtaTracking();
  bindAnalyticsActions();
  activateReveals();
});

window.addEventListener('pagehide', () => {
  syncAnalyticsToAdminApps('pagehide', { keepalive: true, silent: true });
});
